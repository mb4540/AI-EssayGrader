# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Neon Database Configuration
# Get this from your Neon dashboard → Connection Details
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Test Database Configuration (for integration tests)
# Get this from Neon dashboard → Development branch → Connection Details
TEST_DATABASE_URL=postgresql://user:password@host-dev.neon.tech/dbname?sslmode=require

# JWT Secret for Authentication
# Generate a secure random string for production
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Netlify Blobs Configuration
# For local development: Set to 'true' to enable (files won't persist locally)
# For production: Automatically configured by Netlify
ALLOW_BLOB_STORAGE=false

# Netlify Credentials (only needed for local blob storage testing)
# Get these from: Netlify Dashboard → Site Settings → Site Details
# NETLIFY_SITE_ID=your-site-id-here
# NETLIFY_AUTH_TOKEN=your-personal-access-token-here

# Application URL
# For local development
APP_BASE_URL=http://localhost:8888
# For production, set this to your Netlify domain
# APP_BASE_URL=https://your-site.netlify.app
